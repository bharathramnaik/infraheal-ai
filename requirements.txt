# InfraHeal AI — Dependencies
# Install: pip install -r requirements.txt

# LLM Client (talks to vLLM's OpenAI-compatible API)
openai>=1.30.0

# RAG - BM25 retrieval (no GPU needed, saves memory for LLM)
rank-bm25>=0.2.2

# Dashboard
gradio>=4.20.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Utilities
rich>=13.0.0
pydantic>=2.0.0
python-dateutil>=2.8.0
